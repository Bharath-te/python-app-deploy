# python-app-deploy
python app hotel booking deployed Docker file + created image &amp; then contained &amp; deployed using container
                command used:

image created :   docker build -t image1:v1 .
container created: docker run -itd --name mycontmovie2 -p 2222:80 myapp2:v2
